server {
    server_name  terjack.space;
    root /var/www/html;
    auth_basic           'Administrator’s Area';
    auth_basic_user_file /etc/apache2/.htpasswd;

    location /api {
              auth_basic           'Administrator’s Area';
        auth_basic_user_file /etc/apache2/.htpasswd;

    }
}
